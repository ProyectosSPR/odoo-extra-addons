<odoo>
    <record id="account_move_multicompany_rule" model="ir.rule">
        <field name="name">Facturas: Regla multi-empresa</field>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="global" eval="True"/>
        <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
    </record>

    <record id="sale_order_multicompany_rule" model="ir.rule">
        <field name="name">Pedidos de Venta: Regla multi-empresa</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="global" eval="True"/>
        <field name="domain_force">[('company_id', '=', user.company_id.id)]</field>
    </record>

    <record id="res_partner_multicompany_rule" model="ir.rule">
        <field name="name">Contactos: Regla multi-empresa</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="global" eval="True"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', '=', user.company_id.id)]</field>
    </record>
</odoo>